import React from 'react';

const PurchaseDetails = ({tool}) => {
    const {img,name,minimunOrder,quantity,price}=tool;
    return (
        <div>
    
   
        </div>
    );
};

export default PurchaseDetails;